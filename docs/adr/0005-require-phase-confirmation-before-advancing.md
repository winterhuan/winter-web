# Require phase confirmation before advancing

Focus requires Phase Confirmation before starting the next Pomodoro phase instead of automatically running the next timer. This prevents unattended timers from creating false progress and preserves the commitment step before each Pomodoro Cycle.
